import 'package:flutter/material.dart';

/// Global navigator key — lets service-layer code navigate without a BuildContext.
final GlobalKey<NavigatorState> navigatorKey = GlobalKey<NavigatorState>();
